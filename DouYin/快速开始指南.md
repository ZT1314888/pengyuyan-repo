# 抖音数据分析系统 - 快速开始指南

## 方式一：自动初始化（推荐）

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 配置数据库
复制配置文件：
```bash
cp config.py config.py
```

编辑 `config.py`，填入你的MySQL数据库信息：
```python
class Config:
    DB_HOST = 'localhost'
    DB_PORT = 3306
    DB_USER = 'root'  # 你的数据库用户名
    DB_PASSWORD = 'your_password'  # 你的数据库密码
    DB_NAME = 'douyin_analysis'  # 数据库名称
```

### 3. 创建数据库
在MySQL中创建数据库：
```sql
CREATE DATABASE douyin_analysis CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. 一键初始化
运行初始化脚本：
```bash
python init_database.py
```

这个脚本会自动完成：
- ✓ 数据库表结构创建
- ✓ 创建管理员账号（admin/admin123）
- ✓ 创建测试用户（test/test123）
- ✓ 导入示例数据（如果存在）

### 5. 启动服务器
```bash
python manage.py runserver
```

### 6. 访问系统
- 前台系统：http://127.0.0.1:8000/
- 管理后台：http://127.0.0.1:8000/admin/

**默认账号：**
- 管理员：admin / admin123
- 测试用户：test / test123

---

## 方式二：手动初始化

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 配置数据库
```bash
cp config.py config.py
# 编辑config.py填入数据库信息
```

### 3. 创建数据库
```sql
CREATE DATABASE douyin_analysis CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. 执行数据库迁移
```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. 创建管理员账号
```bash
python manage.py createsuperuser
```

### 6. 导入示例数据（可选）
如果有示例数据文件：
```bash
python manage.py loaddata data/fixtures.json
```

### 7. 启动服务器
```bash
python manage.py runserver
```

---

## 常见问题

### Q1: 提示"No module named 'pymysql'"
**解决方案：**
```bash
pip install pymysql
```

### Q2: 数据库连接失败
**检查项：**
1. MySQL服务是否启动
2. config.py中的数据库配置是否正确
3. 数据库是否已创建
4. 用户是否有权限访问数据库

### Q3: 端口8000被占用
**解决方案：**
使用其他端口启动：
```bash
python manage.py runserver 8080
```

### Q4: 静态文件无法加载
**解决方案：**
```bash
python manage.py collectstatic
```

### Q5: 如何重置数据库
**方案1：删除数据库重建**
```sql
DROP DATABASE douyin_analysis;
CREATE DATABASE douyin_analysis CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```
然后重新运行 `python init_database.py`

**方案2：清空数据**
```bash
python manage.py flush
```

---

## 数据管理

### 导出当前数据
如果你想分享你的数据给其他用户：
```bash
python export_data.py
```
这会生成 `data/fixtures.json` 文件

### 导入数据
```bash
python manage.py loaddata data/fixtures.json
```

---

## 开发环境要求

- Python 3.8+
- MySQL 5.7+
- pip

---

## 生产环境部署注意事项

1. **修改SECRET_KEY**
   在 `dy_project/settings.py` 中修改为随机字符串

2. **关闭DEBUG模式**
   ```python
   DEBUG = False
   ```

3. **配置ALLOWED_HOSTS**
   ```python
   ALLOWED_HOSTS = ['your-domain.com', 'www.your-domain.com']
   ```

4. **使用强密码**
   修改默认的admin和test账号密码

5. **配置静态文件**
   ```bash
   python manage.py collectstatic
   ```

6. **使用生产级数据库**
   配置合适的数据库连接池和备份策略

7. **配置HTTPS**
   使用Nginx + SSL证书

---

## 技术支持

如有问题，请提交Issue：
https://github.com/your-username/douyin-analysis/issues

---

**祝使用愉快！** 🎉
